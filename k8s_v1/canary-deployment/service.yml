apiVersion: v1
kind: Service
metadata:
  name: ${APP_NAME}-service-${VERSION}
spec:
  selector:
    app: ${APP_NAME}
    version: ${VERSION_LABEL}
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
